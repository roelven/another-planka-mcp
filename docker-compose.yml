services:
  planka-mcp:
    build: .
    container_name: planka-mcp
    restart: unless-stopped
    ports:
      - "8098:8000"
    env_file:
      - .env
    environment:
      - MCP_TRANSPORT=streamable-http
